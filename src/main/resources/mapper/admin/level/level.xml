<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dksys.biz.admin.level.mapper.LevelMapper">

	<select id="selectLevelList" resultType="CamelMap">
		SELECT
			LEVEL_CODE,
			LEVEL_NAME,
			USE_YN, 
			DATE_FORMAT(CREATE_DT, '%Y-%m-%d %H:%i:%s') AS CREATE_DT,
			DATE_FORMAT(UPDATE_DT, '%Y-%m-%d %H:%i:%s') AS UPDATE_DT
		FROM
			CMN_LEVEL_INFO
		<where>
			<if test="useYn != null and useYn != ''">
				USE_YN = #{useYn}
			</if>
		</where>
	</select>
	
	<select id="selectLevelCount" resultType="CamelMap">
		SELECT
			COUNT(*)
		FROM
			CMN_LEVEL_INFO
		WHERE
			LEVEL_CODE = #{levelCode}
	</select>
	
	<insert id="insertLevel"> 
		INSERT INTO CMN_LEVEL_INFO (
			LEVEL_CODE,
			LEVEL_NAME,
			USE_YN,
			CREATE_ID,
			CREATE_DT,
			UPDATE_ID,
			UPDATE_DT
		) VALUES(
			#{levelCode},
			#{levelName},
			#{useYn},
			#{userId},
			NOW(),
			#{userId},
			NOW()
		)
	</insert>
	
	<select id="selectLevelInfo" resultType="CamelMap">
		SELECT
			LEVEL_CODE,
			LEVEL_NAME,
			USE_YN
		FROM
			CMN_LEVEL_INFO
		WHERE
			LEVEL_CODE = #{levelCode}
	</select>
	
	<update id="updateLevel">
		UPDATE
			CMN_LEVEL_INFO
		SET
			LEVEL_NAME = #{levelName},
			USE_YN = #{useYn},
			UPDATE_DT = NOW()
		WHERE
			LEVEL_CODE = #{levelCode}
	</update>
		
</mapper>