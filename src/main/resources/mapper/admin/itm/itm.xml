<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dksys.biz.admin.itm.mapper.ItmMapper">

	<select id="selectItmList" resultType="CamelMap">
		SELECT * FROM CMN_AUTH_INFO
	</select>

	<insert id="insertItm" parameterType="Map">
		INSERT INTO CMN_AUTH_INFO
		(
			AUTH_ID, AUTH_NM, DESCRIPTION, ROLE_INFO, CREATE_ID, CREATE_DT, UPDATE_ID, UPDATE_DT
		)
		VALUES(
			#{authId}, #{authNm}, #{description}, NULL, #{userId}, SYSDATE(), NULL, NULL
		);		
	</insert>

	<delete id="deleteItm" parameterType="Map">
		DELETE FROM CMN_AUTH_INFO
		WHERE AUTH_ID = #{authId}
	</delete>

	<update id="updateItm" parameterType="Map">
		UPDATE CMN_AUTH_INFO
		SET 
			AUTH_ID = #{authId},
			AUTH_NM = #{authNm},
			DESCRIPTION = #{description},
			UPDATE_ID = #{userId}, 
			UPDATE_DT = SYSDATE()
		WHERE AUTH_ID = #{authId}
	</update>
	
	<select id="selectItmInfo" resultType="CamelMap">
		SELECT
			AUTH_ID,
			AUTH_NM, 
			DESCRIPTION, 
			AUTH_ROLE,
			CREATE_ID,
			CREATE_DT,
			UPDATE_ID,
			UPDATE_DT
		FROM
			CMN_AUTH_INFO
		WHERE
			AUTH_ID = #{authId}
	</select>
</mapper>