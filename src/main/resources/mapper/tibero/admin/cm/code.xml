<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dksys.biz.admin.cm.code.mapper.CodeMapper">

	<select id="selectCodeList" resultType="CamelMap">
		SELECT * FROM CMN_CODE_INFO
	</select>

	<insert id="insertCode" parameterType="Map">
		INSERT INTO CMN_CODE_INFO
		(
			CODE_ID, CODE_NM, CODE_KIND, CODE_DESC, USE_YN, CREATE_ID, CREATE_DT
		)
		VALUES(
			#{codeId}, #{codeNm}, #{codeKind}, #{codeDesc}, 'Y', #{userId}, SYSDATE()
		);		
	</insert>

	<delete id="deleteCode" parameterType="Map">
		DELETE FROM CMN_CODE_INFO
		WHERE CODE_ID = #{codeId}
	</delete>

	<update id="updateCode" parameterType="Map">
		UPDATE CMN_CODE_INFO
		SET 
			CODE_ID = #{codeId},
			CODE_NM = #{codeNm},
			CODE_KIND = #{codeKind},
			CODE_DESC = #{codeDesc},
			UPDATE_ID = #{userId}, 
			UPDATE_DT = SYSDATE()
		WHERE CODE_ID = #{codeId}
	</update>
	
	<select id="selectCodeInfoList" parameterType="Map" resultType="CamelMap">
		SELECT
			CODE_ID, 
			CODE_NM, 
			CODE_KIND, 
			CODE_DESC, 
			USE_YN, 
			CREATE_ID, 
			CREATE_DT
		FROM
			CMN_CODE_INFO
		WHERE
			CODE_KIND = #{codeKind}
	</select>
	 
</mapper>